import type { CollectionConfig } from 'payload'

// Import the working PagesFixed collection
import PagesFixed from './PagesFixed'

// Use the PagesFixed collection configuration
const Pages: CollectionConfig = PagesFixed

export default Pages
