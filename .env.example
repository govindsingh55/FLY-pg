# ===========================================
# Local Development (.env)
# ===========================================
# Copy this file to .env and fill in the values

# Database (local dev uses relative path, Docker uses /app/data/mydb.sqlite)
DB_FILE_NAME=mydb.sqlite

# Server URL (change for production)
ORIGIN=http://localhost:5173
PORT=5173

# Better Auth (REQUIRED - generate a 32+ char random string)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-32-char-secure-random-secret-here
BETTER_AUTH_URL=http://localhost:5173

# ===========================================
# Production / Dokploy Environment Variables
# ===========================================
# Set these in Dokploy App Settings → Environment:
#
# ORIGIN=https://your-domain.com
# PORT=4173
# BETTER_AUTH_SECRET=<generate-secure-secret>
# BETTER_AUTH_URL=https://your-domain.com